import { ButtonConfig } from '@dcx/ui/libs';

export interface FormSummaryButtonsConfig {
  saveButton: ButtonConfig;
  cancelButton: ButtonConfig;
  addButton: ButtonConfig;
  editButton: ButtonConfig;
}
