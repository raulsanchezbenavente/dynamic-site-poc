import { Canvas, Meta } from '@storybook/blocks';

<Meta title="Molecules/Selection Controls/Language Selector/Changelog" />

## [0.1.0]

### Added

- Added new input config property `config` to `LanguageSelector` component

### Changed

- Fix new Dropdown and OptionList accessbility model properties - remove id it is resolved by dropdown and option list components

### Removed

- Removed `convertToDropdownListConfig` function from `LanguageSelector` component
- Removed `buildLanguageListModel` function from `LanguageSelector` component
