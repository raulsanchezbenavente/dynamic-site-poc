// Dynamic key pattern: 'Ibe-codes.FeeCode_' + model.code
// The full key is resolved at runtime by concatenating this prefix with the fee code.
export enum TranslationKeys {
  PriceBreakdown_FeeCode_KeyNode = 'Ibe-codes.FeeCode_',
}
