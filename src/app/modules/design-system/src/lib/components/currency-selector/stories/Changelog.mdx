import { Canvas, Meta } from '@storybook/blocks';

<Meta title="Molecules/Selection Controls/Currency Selector/Changelog" />

## [0.1.0]

### Added

- Added new input config property `config` to `CurrencySelector` component

### Changed

- Fix new Dropdown and OptionList accessbility model properties - remove id it is resolved by dropdown and option list components

### Removed

- Removed `convertToDropdownListConfig` function from `CurrencySelector` component
- Removed `buildCurrencyListModel` function from `CurrencySelector` component
