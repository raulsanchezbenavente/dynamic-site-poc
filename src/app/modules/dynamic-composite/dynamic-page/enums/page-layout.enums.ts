export enum PageLayoutAlignment {
  START = 'start',
  CENTER = 'center',
  END = 'end',
}

export enum PageLayoutBreakpointSize {
  NONE = '',
  XS = 'xs',
  SM = 'sm',
  MD = 'md',
  LG = 'lg',
  XL = 'xl',
}

export enum PageLayoutOrderValue {
  NONE = '',
  FIRST = 'first',
  LAST = 'last',
}