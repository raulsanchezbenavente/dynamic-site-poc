import { SUMMARY_CART_BC_FAKE } from '../../../../../stories/mocks/summary-cart-bc.fake';
import type { SummaryCartConfig } from '../../models/summary-cart.config';

export const DATA_INITIAL_VALUE: SummaryCartConfig = {
  ...SUMMARY_CART_BC_FAKE,
};
