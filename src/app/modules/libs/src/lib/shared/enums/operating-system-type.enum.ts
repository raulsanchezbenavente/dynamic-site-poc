/**
 * Value for each enum key should be reference from UAParserJS library
 */
export enum OperatingSystemType {
  WINDOWS = 'Windows',
  LINUX = 'Linux',
  MAC_OS = 'Mac OS',
  ANDROID = 'Android',
  IOS = 'iOS',
}
