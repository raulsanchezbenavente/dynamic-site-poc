export interface DateFormat {
  day: boolean;
  dayPattern: string;
  week: boolean;
  weekPattern: string;
  month: boolean;
  monthPattern: string;
  year: boolean;
  yearPattern: string;
  fullFormat?: string;
}
