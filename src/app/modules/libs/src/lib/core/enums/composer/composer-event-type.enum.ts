/**
 * Enum to handle composer event types
 * IBE+
 */
export enum ComposerEventTypeEnum {
  SubmitRequested = 'SubmitRequested',
  SubmitFinished = 'SubmitFinished',
  RenderRequested = 'RenderRequested',
  TabNavigated = 'TabNavigated',
}
